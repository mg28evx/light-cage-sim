# Uso en Overleaf

Suba el contenido de esta carpeta preservando `figures/` y seleccione `informe_hueihue.tex` como archivo principal. Antes de emitir el informe, complete en el preámbulo `ClientName`, `RequestedBy` y `CageID`.

